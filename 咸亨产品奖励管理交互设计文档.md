# 咸亨产品奖励管理交互设计文档

> **关联主文档**：《PRD-咸亨产品奖励管理-20260511.md》
> **覆盖范围**：MPM 系统【工作台 → 咸亨产品奖励管理】端到端交互，含工作台入口卡片、奖励配置列表页、详情页、批量导入页、批量修改页。
> **文档定位**：产品/设计/开发对接型——明确页面层级、模块结构、关键交互、状态反馈与异常处理，可直接用于后续原型及网页设计。
> **设计基线**：延续现有 `oms-mpm-物料工作台原型.html` 的 Ant Design 风格工作台语言，作为一个**全新独立模块**设计。
> **配套工作台文件**：`index.html`、`oms-mpm-物料工作台原型.html`（同目录可双击打开预览）。

---

## 版本记录

| 版本 | 发布日期 | 变更说明 |
|---|---|---|
| **1.0** | 2026-05-11 | 首版创建。覆盖：① 工作台新增独立业务概览卡；② 奖励配置列表页结构与状态快捷切换；③ 奖励配置详情页的类型联动、时间约束、保存反馈；④ 批量导入与批量修改分流设计；⑤ 同步状态、异常处理与权限交互。 |

---

## 0. 全局架构

### 0.1 页面跳转关系

```
┌──────────────────────────────┐
│ /物料工作台（工作台首页）      │
│ - 物料管理                    │
│ - 咸亨产品奖励管理（新增）    │
│ - 今日待办                    │
└──────────────┬───────────────┘
               │ 点击“咸亨产品奖励管理”概览卡
               ▼
┌──────────────────────────────────────────────┐
│ /咸亨产品奖励管理                            │
│ 奖励配置列表页（模块默认首页）               │
│ 顶部快捷状态：全部 / 生效中 / 异常           │
│ 操作区：新增奖励配置 / 批量导入 / 批量修改   │
└──────────────┬───────────────┬──────────────┘
               │               │
               │ 点击“新增/编辑/查看”
               │               ▼
               │    ┌────────────────────────────┐
               │    │ /奖励配置详情页            │
               │    │ - 基础信息                 │
               │    │ - 奖励规则信息             │
               │    │ - 时间与展示规则           │
               │    │ - 状态与操作信息           │
               │    └────────────────────────────┘
               │
               ├── 点击“批量导入”
               │   ▼
               │  ┌────────────────────────────┐
               │  │ /批量导入页                │
               │  │ - 模板下载                 │
               │  │ - 上传导入                 │
               │  │ - 导入结果                 │
               │  └────────────────────────────┘
               │
               └── 勾选数据后点击“批量修改”
                   ▼
                  ┌────────────────────────────┐
                  │ /批量修改页                │
                  │ - 已选数据摘要             │
                  │ - 修改项选择               │
                  │ - 字段填写                 │
                  │ - 变更确认                 │
                  └────────────────────────────┘
```

### 0.2 模块定位

| 维度 | 定位 |
|---|---|
| 模块属性 | 全新独立模块，不挂靠在现有“物料管理”功能入口中 |
| 工作台层级 | 以独立业务概览卡形式展示，与“物料管理”同级 |
| 模块首页 | 点击概览卡后直接进入“奖励配置列表页” |
| 核心交互方式 | 列表主控 + 详情承载 + 批量分流 + 状态可感知 |

### 0.3 角色与权限

| 角色 | 列表页 | 详情页 | 批量导入/修改 |
|---|---|---|---|
| **运营维护角色** | 可查看、筛选、切换状态、查看同步结果 | 可新增、编辑、作废 | 可导入、批量修改、批量作废 |
| **只读角色** | 可查看列表、状态、同步结果 | 仅可查看详情 | 不可操作 |
| **管理/支持角色** | 同运营维护角色 + 可查看异常原因 | 可执行重试同步 | 可查看批量操作结果与异常 |

> 权限不足时，建议直接隐藏不可用按钮；如因页面统一性需要保留入口，则按钮置灰并提示“暂无权限”。

---

## 1. 工作台入口交互

### 1.1 入口定位
在现有 `物料工作台` 页面中，新增一张与“物料管理”并列的独立卡片：

**卡片名称：咸亨产品奖励管理**

该卡片为**业务概览卡**，用于体现这是一个独立运营模块，而不是某个附属功能入口。

### 1.2 工作台布局建议
当前工作台下半区为：
- 左侧：`物料管理`
- 右侧：`今日待办`

本次建议调整为更适合新模块的双卡布局：
- 第一行：
  - 左侧：`物料管理`
  - 右侧：`咸亨产品奖励管理`
- 第二行：
  - 继续保留 `今日待办`，可独立成整行，或在页面空间允许时作为辅助区保留在侧边

### 1.3 概览卡内容
概览卡不做过多入口，重点展示 4 类业务指标：
1. 奖励配置总数
2. 当前生效中数量
3. 即将到期数量
4. 异常同步数量（商城同步失败、BI 同步失败等）

### 1.4 概览卡交互组成
| 区域 | 内容 | 交互说明 |
|---|---|---|
| 卡片头部 | 模块标题 + 奖励图标 | 和物料管理卡片同风格，强调独立模块身份 |
| 卡片主体 | 4 个概览指标 | 支持点击某项后带筛选条件进入列表页 |
| 卡片底部 | 主按钮 `进入奖励管理` | 点击后进入列表页 |
| 辅助入口 | `查看生效中`、`查看异常数据`（建议保留） | 作为轻量快捷入口，不喧宾夺主 |

### 1.5 点击行为
| 触发元素 | 行为 | 目标页面 |
|---|---|---|
| 卡片空白区 | 进入模块默认首页 | 奖励配置列表页 |
| `进入奖励管理` 按钮 | 进入模块默认首页 | 奖励配置列表页 |
| `查看生效中` | 带状态过滤进入 | 列表页（生效中） |
| `查看异常数据` | 带状态过滤进入 | 列表页（异常） |

---

## 2. 模块信息架构

### 2.1 一级结构
模块主路径固定为：

**工作台概览卡 → 奖励配置列表页 → 奖励配置详情页**

### 2.2 首期页面清单
| 页面 | 定位 | 说明 |
|---|---|---|
| 奖励配置列表页 | 模块默认首页 | 负责查询、筛选、状态切换、单条与批量操作分发 |
| 奖励配置详情页 | 单条维护页 | 承载新增、编辑、查看三种场景 |
| 批量导入页 | 批量写入页 | 提供模板下载、文件上传、结果反馈 |
| 批量修改页 | 批量变更页 | 对已选奖励配置做统一字段修改或批量作废 |

### 2.3 首期不单独拆出的页面
首期不建议单独做以下页面，避免模块失焦：
- 奖励首页仪表盘
- 独立同步监控页
- 独立操作日志页
- 独立规则配置页

核心目标是先保证“配置维护链路”顺畅，而不是构建经营分析平台。

---

## 3. 列表页交互设计

### 3.1 页面定位
列表页是模块默认首页，承担以下 5 类任务：
1. 查看当前奖励配置
2. 通过状态切换和筛选快速定位数据
3. 发起单条新增/编辑
4. 发起批量导入、批量修改
5. 处理作废、查看异常状态与同步结果

### 3.2 页面结构
页面建议从上到下分为 5 个区域：
1. 页面标题区
2. 状态快捷切换区
3. 筛选区
4. 列表表格区
5. 表格工具栏与行内操作区

### 3.3 页面标题区
| 元素 | 内容 |
|---|---|
| 页面标题 | 咸亨产品奖励管理 |
| 副文案 | 用于维护项目奖励、金额奖励、比例奖励 |
| 右侧主按钮 | `新增奖励配置` |
| 右侧次按钮 | `批量导入`、`批量修改` |

按钮层级建议：
- 主按钮：`新增奖励配置`
- 次按钮：`批量导入`
- 次按钮：`批量修改`

### 3.4 状态快捷切换区
顶部只保留少量高价值状态：
- `全部`
- `生效中`
- `异常`

其中“异常”是运营视角聚合状态，包含：
- 商城同步失败
- BI 同步失败
- 配置字段异常
- 展示时间/有效期异常
- 冲突数据

### 3.5 筛选区
#### 默认展示字段
- 物料编码
- 物料名称
- 奖励类型
- 配置状态
- 奖励有效期
- 商城展示时间

#### 展开后字段
- 项目编码
- 创建时间
- 更新时间
- 商城同步状态
- BI 同步状态

#### 按钮
- 查询
- 重置
- 展开/收起

### 3.6 表格字段建议
| 顺序 | 字段 | 说明 |
|---|---|---|
| 1 | 多选框 | 支持批量修改、批量作废 |
| 2 | 物料编码 | 主要识别字段 |
| 3 | 物料名称 | 文本 |
| 4 | 奖励类型 | 项目奖励 / 金额奖励 / 比例奖励 |
| 5 | 项目编码 | 项目奖励展示具体值或“无项目编码” |
| 6 | 奖励金额 | 非金额奖励展示 `-` |
| 7 | 奖励比例 | 非比例奖励展示 `-` |
| 8 | 奖励有效期 | 时间区间 |
| 9 | 商城展示时间 | 时间区间 |
| 10 | 配置状态 | 未生效 / 生效中 / 已失效 / 已作废 |
| 11 | 商城同步状态 | 成功 / 失败 |
| 12 | BI 同步状态 | 成功 / 失败 |
| 13 | 更新时间 | 最近更新时间 |
| 14 | 操作 | 查看 / 编辑 / 作废 / 重试同步 |

### 3.7 行内操作
| 操作 | 行为 |
|---|---|
| 查看 | 进入详情页只读模式 |
| 编辑 | 进入详情页可编辑模式 |
| 作废 | 放入“更多”操作中，二次确认后执行逻辑作废 |
| 重试同步 | 仅在同步失败时展示 |

### 3.8 空态与异常态
#### 空数据态
- 提示暂无奖励配置
- 提供 `新增奖励配置`、`批量导入` 按钮

#### 无查询结果态
- 提示未找到符合条件的数据
- 支持一键清空筛选

#### 异常态
- 进入“异常”快捷状态时，列表上方展示异常提示条
- 明确标示异常类型，如商城同步失败、BI 同步失败、配置冲突等

---

## 4. 奖励配置详情页交互设计

### 4.1 页面定位
详情页承载三类场景：
1. 新增奖励配置
2. 编辑奖励配置
3. 查看奖励配置详情

建议三者共用同一页面骨架，通过模式控制字段可编辑/只读。

### 4.2 页面整体结构
详情页建议分为 4 个区块：
1. 基础信息
2. 奖励规则信息
3. 时间与展示规则
4. 状态与操作信息

### 4.3 基础信息区
| 字段 | 新增场景 | 编辑场景 | 查看场景 |
|---|---|---|---|
| 物料编码 | 可输入/选择 | 默认不可改 | 只读 |
| 物料名称 | 自动带出，只读 | 只读 | 只读 |
| 奖励类型 | 可选 | 可切换 | 只读 |

交互原则：先锁定物料对象，再进入规则配置。

### 4.4 奖励规则信息区
#### 项目奖励
展示字段：
- 项目编码（可空）
- 奖励说明文案

不展示：
- 奖励金额
- 奖励比例

#### 金额奖励
展示字段：
- 奖励金额（带“元”单位）
- 奖励说明文案（选填）

不展示：
- 项目编码
- 奖励比例

#### 比例奖励
展示字段：
- 奖励比例（带 `%` 单位）
- 奖励说明文案（选填）

不展示：
- 项目编码
- 奖励金额

弱提示：
- “比例奖励与项目调拨价相关，MPM 本期仅维护比例值，不参与结算计算”

### 4.5 时间与展示规则区
字段：
- 奖励有效期开始时间
- 奖励有效期结束时间
- 商城展示开始时间
- 商城展示结束时间

交互规则：
- 先选奖励有效期，再选商城展示时间
- 商城展示时间必须完全落在奖励有效期内
- 若有效期修改导致展示时间超范围，应即时提示并要求重新选择

辅助提示：
- 奖励有效期：决定奖励配置业务上是否生效
- 商城展示时间：决定奖励信息何时在商城展示

### 4.6 状态与操作信息区
建议展示：
- 配置状态
- 商城同步状态
- BI 同步状态
- 创建人 / 创建时间
- 更新人 / 更新时间

### 4.7 页面底部操作区
#### 新增场景
- 保存
- 保存并继续新增
- 取消

#### 编辑场景
- 保存
- 作废
- 取消

#### 查看场景
- 编辑
- 返回

### 4.8 关键联动规则
1. 奖励类型切换时动态展示对应字段
2. 跨类型切换时自动清空无关字段
3. 展示时间受有效期约束
4. 物料编码锁定后再配置规则

### 4.9 校验反馈层级
| 层级 | 类型 | 示例 |
|---|---|---|
| 第一层 | 字段级即时校验 | 金额为空、比例为空、时间格式错误 |
| 第二层 | 区块级提醒 | 展示时间超出有效期、切换类型已清空字段 |
| 第三层 | 提交级校验 | 同一物料同一有效期存在其他奖励类型配置 |

---

## 5. 批量导入交互设计

### 5.1 入口位置
列表页右上角独立按钮：`批量导入`

点击后进入独立页面，而不是弹窗。

### 5.2 页面结构
批量导入页建议分为 4 个区域：
1. 导入说明区
2. 模板下载区
3. 上传区
4. 导入结果区

### 5.3 导入说明区
说明要点：
- 支持操作类型：新增 / 修改 / 作废
- 导入前请先下载模板
- 修改 / 作废时需填写奖励配置 ID
- 导入数据执行与页面一致的校验规则

### 5.4 模板下载区
提供：
- `下载导入模板`
- `下载填写示例`
- `查看字段说明`

### 5.5 上传区
展示：
- 拖拽上传
- 点击上传
- 支持格式说明（如 `.xlsx`）
- 上传后展示文件名、上传时间、上传人

### 5.6 导入结果区
结果摘要展示：
- 总条数
- 成功条数
- 失败条数

操作按钮：
- `下载失败明细`
- `返回列表`
- `继续导入`

失败明细至少包含：
- 行号
- 物料编码
- 操作类型
- 失败原因

### 5.7 导入状态反馈
导入过程拆分为：
1. 待上传
2. 校验中
3. 导入成功
4. 部分成功 / 导入失败

如果“部分成功”，必须明确：
- 成功数据已生效
- 失败数据未入库
- 可下载失败明细后重新导入

---

## 6. 批量修改交互设计

### 6.1 入口位置
列表页右上角独立按钮：`批量修改`

进入前需勾选数据，未勾选则提示：
- “请先选择需要批量修改的奖励配置”

### 6.2 页面结构
批量修改采用独立页面，分为 4 个区域：
1. 已选数据摘要
2. 修改项选择区
3. 字段填写区
4. 变更确认区

### 6.3 已选数据摘要
展示：
- 已选条数
- 奖励类型分布
- 配置状态分布
- 是否存在跨类型数据

### 6.4 修改项选择区
先勾选本次要修改的字段：
- 奖励类型
- 奖励金额
- 奖励比例
- 奖励有效期
- 商城展示时间
- 奖励说明文案
- 批量作废

只有勾选的字段才展示对应输入控件。

### 6.5 字段填写区
根据勾选项动态出现控件。

跨类型修改时需要明确提示：
- “修改后将清空与新奖励类型不匹配的字段”

### 6.6 变更确认区
提交前展示摘要，例如：
- 已选 36 条奖励配置
- 统一改为：比例奖励
- 奖励比例统一设置为：3%
- 商城展示时间统一调整为：2026-06-01 至 2026-12-31

### 6.7 批量作废建议
虽然可作为批量修改的一种操作，但交互上更建议独立成风险操作入口：
- `批量作废`

作废前二次确认并明确影响：
- 商城不再展示
- BI 不再作为当前有效配置汇总
- 历史记录保留

### 6.8 风险控制点
1. 未选数据不可进入批量修改
2. 跨类型修改时必须提示清空无关字段
3. 时间修改必须继续校验展示时间 ⊂ 有效期
4. 提交前增加批量确认摘要
5. 超过一定条数时强化确认提示

---

## 7. 状态反馈与异常处理

### 7.1 三层反馈机制
#### 第一层：操作反馈
用于提示：
- 新增成功
- 编辑成功
- 作废成功
- 批量修改成功
- 导入完成

#### 第二层：业务结果反馈
用于持续展示：
- 未生效
- 生效中
- 已失效
- 已作废

#### 第三层：同步反馈
用于单独展示：
- 商城同步成功 / 失败
- BI 同步成功 / 失败

### 7.2 异常分类与处理
| 异常类型 | 示例 | 处理方式 |
|---|---|---|
| 字段输入异常 | 金额为空、比例为空、时间非法 | 字段下红字提示，不允许提交 |
| 规则冲突异常 | 同物料同时间存在其他奖励类型、展示时间超有效期 | 区块级提示 + 提交失败说明 |
| 同步异常 | 商城同步失败、BI 同步失败 | 列表与详情页展示失败状态，支持重试同步 |
| 导入异常 | 模板字段缺失、物料编码不存在、配置 ID 不存在 | 导入结果页集中反馈，支持下载失败明细 |

### 7.3 异常页签与回路
列表页切换到 `异常` 状态后，应明确展示：
- 异常类型
- 失败原因
- 处理入口（查看详情 / 重试同步）

---

## 8. 交互动线总结

### 8.1 单条新增
工作台 → 点击“咸亨产品奖励管理”概览卡 → 进入列表页 → 点击“新增奖励配置” → 进入详情页填写 → 保存成功 → 返回列表查看状态与同步结果

### 8.2 单条编辑
列表页 → 查询定位配置 → 点击“编辑” → 进入详情页修改 → 保存 → 返回列表查看最新状态

### 8.3 批量导入
列表页 → 点击“批量导入” → 下载模板 / 填写模板 → 上传文件 → 查看导入结果 → 下载失败明细（如有）→ 返回列表确认结果

### 8.4 批量修改
列表页 → 勾选多条配置 → 点击“批量修改” → 查看已选摘要 → 选择修改字段并填写 → 查看确认摘要 → 提交成功 → 返回列表查看结果

### 8.5 异常处理
列表页 → 切换到“异常” → 查看异常类型与失败原因 → 进入详情或执行重试同步 → 异常消除后回到正常状态

---

## 9. 首期交互边界建议

### 9.1 本期必须体现
- 工作台新增独立概览卡
- 奖励配置列表页
- 奖励配置详情页
- 批量导入页
- 批量修改页
- 异常状态感知
- 同步状态展示

### 9.2 本期可只留占位或文案
- 操作日志详情
- 同步失败详情弹层
- 消息通知中心联动
- 独立经营分析仪表盘

---

## 10. 视觉与结构原则摘要

### 10.1 视觉原则
| 项目 | 原则 |
|---|---|
| 风格 | 延续现有工作台 Ant Design 风格 |
| 模块定位 | 新模块需有独立卡片身份感 |
| 列表页 | 强调效率、查询、状态感知 |
| 详情页 | 强调规则清晰、字段联动、错误前置 |
| 批量页 | 强调范围确认、风险控制、结果回收 |

### 10.2 交互原则
1. 列表为中心，详情为承载
2. 批量导入与批量修改明确分流
3. 配置状态与同步状态分离展示
4. 复杂规则尽量前置联动，不依赖最后报错
5. 风险操作必须二次确认

---

> **说明**：本文档为文字交互方案，已明确模块结构、页面层级与主要行为规则。待业务确认后，可据此进入下一步网页设计与原型实现阶段。