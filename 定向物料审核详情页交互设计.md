# 定向物料审核交互设计文档

> **关联主文档**：《OMS-MPM 定向物料创建需求文档.md》v1.4
> **覆盖范围**：MPM 系统【任务中心 → 待我审核 → 定向物料新增审核】端到端交互，含列表页、详情页、类目选择弹窗。
> **文档定位**：开发对接型——逐字段权限/校验/接口映射、可直接据此实现。
> **设计基线**：参考 MPM 现有「物料信息-审核-编辑」页框架，叠加定向物料专属能力。
> **配套原型文件**：`待我审核.html`、`物料审核详情.html`（同目录可双击打开预览）。

---

## 版本记录

| 版本 | 发布日期 | 变更说明 |
|---|---|---|
| **2.0** | 2026-04-27 | **全量重写**取代 v1.0。基于现有 HTML 原型与需求文档 v1.4 同步对齐。覆盖：① 列表页（子 Tab、列字段、跳转、批量驳回弹窗、定向 Tab 引导抖动）；② 详情页（4 列布局、9 大字段区块、AI 类目字段+引导动画、价格联动、通过/驳回流）；③ 类目选择弹窗（左侧树形+右侧 AI 建议前 3 候选+置信度可视化+重试机制）；④ 字段-接口映射；⑤ 错误文案；⑥ 异常场景。 |

---

## 0. 全局架构

### 0.1 页面跳转关系

```
┌────────────────────┐
│  /oms-mpm-工作台    │
│  （物料工作台首页） │
└──────────┬─────────┘
           │ 点击"待我审核"卡片
           ▼
┌────────────────────────────────────────────────┐
│  /待我审核                                       │
│  顶部子 Tab：                                    │
│  新增审核 / 修改审核 / MDM / SRM / IQS /         │
│  ⭐定向物料新增审核（橙色抖动引导，首访高亮）/ 全部│
└──────────┬─────────────────────────────────────┘
           │ 点击表格内"咸亨物料编码"列
           │ （仅定向物料新增审核 Tab 可点击）
           ▼
┌────────────────────────────────────────────────┐
│  /物料审核详情?code={LINE_xxx}                   │
│  - 顶部操作栏：返回 / 分配组织 / 复制 / 关闭 /   │
│    驳回（红） / 通过（绿）                       │
│  - 主体：主要信息 / 次要信息 / 商品标签 /        │
│    默认信息 / 外部链接 / 价格佐证 / 拓展属性 /   │
│    资料附件 / 图片                               │
│  - 类目字段首次访问触发橙色脉冲 + 浮动气泡引导   │
└──────────┬─────────────────────────────────────┘
           │ 点击"类目"输入框
           ▼
┌────────────────────────────────────────────────┐
│  类目选择弹窗（modal，宽 880px）                 │
│  顶部：搜索框                                    │
│  左侧：📂 类目层级（懒加载树形，必须选到四级）    │
│  右侧：🤖 四级类目（AI 建议）                    │
│        - 前 3 候选 + 置信度进度条 + 判定理由     │
│        - 🔄 重试按钮（重新调用接口4）            │
│        - "✓ 采用推荐"（第1名）/ "采用"（第2、3）  │
└────────────────────────────────────────────────┘
```

### 0.2 角色与权限

| 角色 | 列表页 | 详情页 |
|---|---|---|
| **MPM 审核员** | 可见所有审核任务、可点击物料编码进详情 | 可编辑字段、通过、驳回 |
| **OMS 销售（发起人）** | 不进入此页 | — |
| **管理员** | 同审核员 + 可分配审核人 | 同审核员 |

---

## 1. 列表页交互（待我审核.html）

### 1.1 页面结构

```
┌──────────────────────────────────────────────────────────┐
│ [咸亨主数据平台]  消息 价格审核 ... 任务中心(9+)  管理员  │
├──────────────────────────────────────────────────────────┤
│ Tabs：工作台 | 待我审核(active) | 审核结果        清除   │
├──────────────────────────────────────────────────────────┤
│ 筛选区：货主 ▾ | 申请人 | 物料名称 | 物料等级 ▾ |        │
│           内部备注 | [查询] [重置]                       │
├──────────────────────────────────────────────────────────┤
│ 操作栏：操作：[刷新] [批量通过(绿)] [批量驳回(红,定向Tab隐藏)] [导出] │
├──────────────────────────────────────────────────────────┤
│ 子 Tab：新增审核 修改审核 MDM SRM新增二审 SRM修改二审    │
│         IQS新增审核 ⭐定向物料新增审核 全部              │
├──────────────────────────────────────────────────────────┤
│ 表格（横滑，最小宽 2800px）：                            │
│ ☐ | 货主 | 咸亨物料编码 | 名称 | 四级类目 | 型号 ...     │
├──────────────────────────────────────────────────────────┤
│ 分页：共 1283 条 ‹ 1 2 3 4 5 ... 52 › | 25 条/页 | 跳转 │
└──────────────────────────────────────────────────────────┘
```

### 1.2 子 Tab 行为

| 子 Tab | 说明 |
|---|---|
| **⭐ 定向物料新增审核** | **本次新增** |

> 其他子 Tab（新增审核 / 修改审核 / MDM / SRM 新增二审 / SRM 修改二审 / IQS 新增审核 / 全部）沿用现有逻辑，不在本次需求范围内。

### 1.3 跳转规则（核心）

| 触发元素 | 当前 Tab | 跳转方式 | 目标 URL |
|---|---|---|---|
| 表格内「咸亨物料编码」单元格 | 定向物料新增审核 | 当前窗口跳转（沿用 `goDetail()`，第 580 行） | `物料审核详情.html?code=LINE_xxx` |
| 表格内「咸亨物料编码」单元格 | 其他子 Tab | **不响应**（普通文本，无 hover） | — |

**渲染逻辑**（已在原型实现）：

```js
${r.auditType === '定向物料新增审核'
  ? `<span class="cell-ellipsis td-link" onclick="goDetail('${r.code}')">${r.code}</span>`
  : `<span class="cell-ellipsis">${r.code}</span>`}
```

### 1.4 列字段清单（定向 Tab）

> 表格采用 `min-width: 2800px` + 横向滚动，确保所有列可见。

| 列名 | 数据源 | 渲染规则 |
|---|---|---|
| ☐ 多选 | — | 复选框，全选联动 |
| 货主 | `r.owner` | 文本截断（最多 120px） |
| **咸亨物料编码** | `r.code` | **默认展示定向函行ID**（例 `LINE_xxx`）；蓝色超链接，点击跳转详情页 |
| 名称 | `r.name` | 文本截断 |
| 四级类目 | `r.cat` | 有值时正常显示；空值显示红标签「AI 未匹配」 |
| 型号 / 规格 / 描述 / 单位 / 品牌 / 物料等级 | 同名字段 | 文本，长字段截断 80px |
| 等级（grade） / 订单执行员 / 采购部门 / 内部备注 | 同名字段 | 同上 |
| 物料类型 / 图片类型 | 标签渲染 | 「无图物料」红标签；其他类型蓝标签 |
| 驳回理由 | `r.rejectReason` | 文本，无内容显示 `-` |
| 提交申请时间 | `r.applyTime` | 格式 `YYYY-MM-DD HH:mm` |
| 申请人 / 助记码 / 项目 / 主图数量 / 详情图数量 | 同名字段 | 数字或文本 |
| 物料标签 / 审核人 | 同名字段 | 文本 |
| AI 审核建议 | `r.aiSuggest` | **定向 Tab 显示 `-`**；其他 Tab 渲染绿/红/橙标签 |
| AI 审核结论 | `r.aiConclusion` | 同上规则 |

### 1.5 筛选与操作

| 控件 | 行为 |
|---|---|
| 货主下拉 | 选项：万聚国际（杭州）/ 威亨国际科技 / 浙江威亨创新产 |
| 申请人 / 物料名称 / 内部备注 | 文本输入，模糊匹配（前端示例不实现） |
| 物料等级下拉 | 选项：零星 / 常规 |
| 查询按钮 | Toast「查询中...」 |
| 重置按钮 | 清空所有筛选项 + Toast |
| 刷新 | Toast「已刷新列表」 |
| **批量通过** | 至少勾选 1 条，否则 Toast「请先勾选物料」；通过后 Toast 反馈 |
| **批量驳回** | **定向 Tab 隐藏**；其他 Tab 弹批量驳回 Modal |
| 导出 | Toast「正在导出...」 |

### 1.6 批量驳回弹窗（非定向 Tab）

```
┌──────────────────────────────────────────────────────┐
│ 批量驳回                                          ×  │
├──────────────────────────────────────────────────────┤
│ ┌────────────────────────────────────────────────┐  │
│ │ 外部编码 | 名称 | 型号 | 规格 | 驳回原因          │  │
│ ├────────────────────────────────────────────────┤  │
│ │ A2607001789 | 螺旋测微仪 | 0-25mm | - | [输入]🔍 │  │
│ │ A0201005032 | 钳形电流表 | CM-9930 | - | [输入]🔍 │  │
│ │ ...                                              │  │
│ └────────────────────────────────────────────────┘  │
│ ☐ 统一驳回理由   [输入框（统一勾选后启用）        🔍] │
├──────────────────────────────────────────────────────┤
│                              [确定]      [取消]      │
└──────────────────────────────────────────────────────┘
```

| 控件 | 行为 |
|---|---|
| 每行驳回原因 | 必填，逐行独立 |
| ☐ 统一驳回理由 | 勾选后启用统一输入框，输入内容**实时同步**到所有行；取消勾选后清空所有行 |
| 确定 | 校验所有行驳回原因非空，否则 Toast 警告 |

---

## 2. 详情页交互（物料审核详情.html）

### 2.1 顶部固定区（高度 128px）

```
┌──────────────────────────────────────────────────────────┐
│ [咸亨主数据平台]  ... 任务中心(9+)  管理员           48px│
├──────────────────────────────────────────────────────────┤
│ Tabs：工作台 | 待我审核 | 物料查看·审核-{code}(active)36px│
├──────────────────────────────────────────────────────────┤
│ ← 返回列表 | 待我审核 > 物料查看·审核    [分配组织][复制]│
│                                          [关闭][驳回][通过]44px│
└──────────────────────────────────────────────────────────┘
```

| 操作按钮 | 类型 | 行为 |
|---|---|---|
| ← 返回列表 | 默认按钮 | 跳转 `待我审核.html` |
| 分配组织 | 默认按钮 | 保留历史功能 |
| 复制 | 默认按钮 | 保留历史功能 |
| 关闭 | 默认按钮 | 跳转 `待我审核.html` |
| **驳回** | 危险（红） | 弹驳回 Modal，要求驳回理由非空 |
| **通过** | 成功（绿） | 直接 Toast「审核通过：{code}」（本期未做字段全量校验，正式版需校验） |

### 2.2 主要信息区（4 列布局）

#### 2.2.1 字段表（与参考图 1:1 对齐）

| 行 | 字段 | 占列 | 必填 | 类型/默认值 | 可编辑 | 数据来源 |
|---|---|---|---|---|---|---|
| 1 | 货主 | 2 | — | 只读，固定 `万聚国际（杭州）供应链有限公司` | ❌ | MPM 内部货主固定 `1025`（主文档 §0） |
| 1 | 物料标签 | 2 | ★ | 单选，默认勾选「定向物料」 | ✅ | 自营物料 / **定向物料** / 电商服务商 |
| 2 | 编码 | 1 | — | 只读，默认展示**定向单行号编码**，例 `DXH-XXXXXX` | ❌ | OMS 推送的定向单行号；审核通过后系统替换为正式咸亨物料编码 |
| 2 | 品牌 | 1 | ★ | 下拉，例 `钢炬/GANGJU` | ✅ | OMS `material.brandId` → MPM 品牌库 |
| 2 | 名称 | 1 | ★ | 文本，例 `PVC管` | ✅ | OMS `material.itemName`（1-40 字符规则同主文档 §8.1） |
| 2 | 型号 | 1 | ★ | 文本，例 `DN110*4m` | ✅ | OMS `material.model` |
| 3 | **基本单位** | 1 | ★ | 文本，例 `米`，**右上角红字「⚠ 审核后无法修改」** | ✅（仅审核阶段） | OMS `material.unitId` |
| 3 | **类目** | 1 | ★ | 只读输入框（点击弹窗），绿底 + 「AI推荐」橙标签 | ✅（弹窗选择） | 接口4 `data.category_name` + `data.id` |
| 3 | 订单执行员 | 1 | — | 下拉「请选择 / 胡乾礼-通用工具采购组 / ...」 | ✅ | 类目联动赋值 |
| 3 | 采购部门 | 1 | ★ | 下拉「请选择 / 通用工具采购组 / ...」 | ✅ | 类目联动赋值 |
| 4 | 项目报价员 | 1 | — | 下拉 | ✅ | 类目联动赋值 |
| 4 | 产品经理 | 1 | — | 下拉 | ✅ | 类目联动赋值 |
| 4 | **加价系数(%)** | 1 | ★ | 数字，默认 `0.00` | ✅ | 类目联动赋值（可手动改） |
| 4 | **物料税率** | 1 | ★ | 下拉，默认 `13%增值税`，选项 `9% / 6% / 0%` | ✅ | 类目联动赋值 |
| 5 | **起订量** + ☐ 是否倍数下单 | 1（addon） | — | 数字 + 复选 | ✅ | OMS `material.quantity` |
| 5 | **毛重** | 1（addon） | ★ | 数字 + `kg` 后缀 | ✅ | 默认 `1` |
| 5 | 别名 | 1 | — | 文本，占位「请输入」 | ✅ | — |
| 5 | （空） | 1 | — | — | — | — |

#### 2.2.2 价格区（黄底 `#fffbe6`，体现"敏感字段"语义）

| 行 | 字段 | 占列 | 必填 | 类型 | 联动规则 |
|---|---|---|---|---|---|
| 6 | **市场价** | 1 | ★ | 数字 + 元后缀 | **市场价 = 进货单价 × 1.5（四舍五入 2 位）**（主文档 §8.2）|
| 6 | **商城价** | 1 | ★ | 数字 + 元 + 「查询」链接 + ☑ 自动计算 | 勾选「自动计算」时由市场价×加价系数公式赋值 |
| 6 | 结算价 | 1 | — | ⓘ + 占位「待询价」（只读灰字） | 询价完成后由系统填入 |
| 6 | 项目调拨参考价 | 1 | — | ⓘ + 占位「待询价」（只读灰字） | 同上 |
| 7 | 参考供货价 | 1 | — | ⓘ + 占位「待询价」（只读灰字） | 同上 |

#### 2.2.3 规格区

| 行 | 字段 | 占列 | 必填 |
|---|---|---|---|
| 8 | 规格 | 1 | — |
| 8 | 对外最高成交价 | 1 + 元 | — |
| 8 | 对内最新成本价 | 1 + 元 | — |
| 8 | 品牌限价最低价 | 1 + 元 | — |
| 9 | 品牌限价最高价 | 1 + 元 | — |

#### 2.2.4 描述与选项区

| 行 | 字段 | 占列 | 必填 | 默认值 |
|---|---|---|---|---|
| 10 | 描述（textarea） | 2 | — | 例 `4米/根` |
| 10 | 内部备注（textarea） | 2 | — | **预填「复制生成物料（源物料编码B2002079386）」**或 `定向函编码：{batchId}` |
| 11 | 成本是否含运费 | 2 | ★ | 单选「●不含 ○含运费」 |
| 11 | 检测费 | 2 | — | 复选「不含检测费 / 含第三方检测费 / 含出厂检测费」 |
| 12 | 安装费 | 2 | — | 复选「含 / 不含」 |

### 2.3 字段联动规则

#### 2.3.1 类目变更联动

类目修改后**必须**重新拉取以下字段：

| 联动字段 | 重算依据 | 主文档锚点 |
|---|---|---|
| 订单执行员 / 采购部门 / 项目报价员 / 产品经理 | 类目+品牌+标签+货主 | §8.2 |
| 加价系数 | 同上 | §8.2 |
| 税收分类名称 / 税收分类编码 / 简称 / 税率 / 物料税率 | 类目 | §8.2 |
| 是否有效期管理 | 类目+品牌+标签+货主 | §8.2 |
| 拓展属性项（外径/壁厚等） | 类目 | §8.2 |

> **冲突提示**：审核员已手动修改的字段，类目变更时弹「已修改的字段将被覆盖，是否继续？」二次确认。

#### 2.3.2 价格联动

```
进货单价（OMS 推送）
      │ × 1.5（保留2位）
      ▼
   市场价 ────────────┐
                     │ × (1 + 加价系数%)
   ☑ 自动计算时 ─────►│
                     ▼
                   商城价
```

- 「市场价」字段被审核员手动改写后，**不再随进货单价联动**（一次性手动接管）
- 「商城价」「☑ 自动计算」取消勾选后，可手动改写

### 2.4 必填项校验

| 触发时机 | 行为 |
|---|---|
| 字段失焦（blur） | 实时校验，错误时红色边框 + 字段下方提示 |
| 点击「通过」 | 全表全量校验，定位首个错误字段 + 自动滚动 + 聚焦 |
| 校验文案 | 沿用主文档 §8.1 错误描述（例「请输入中英文数字标点符号；最短1字，最长40个字」） |

### 2.5 通过流

```
点击 [通过]
   │
   ▼
全量校验必填项 ── 失败 → 滚动到首个错误字段，红框+提示
   │ 通过
   ▼
弹「确认审核通过？」二次确认（差异预览：审核员修改的字段 high-light）
   │ 取消 → 返回
   │ 确认
   ▼
后端：
   ① 字段规范校验 + 字段读取逻辑校验
   ② 生成新咸亨物料编码（覆盖临时码）
   ③ 写入 omsMaterialId → mpmMaterialCode 关系表
   ④ 立即触发主文档 §11 接口3 单条回传 OMS（resultSource=AUDIT_NEW）
   ▼
前端 Toast 「✅ 审核通过：{code}」
   ▼
自动关闭 Tab（或下拉「通过并继续审核下一条」自动加载下一条）
```

### 2.6 驳回流

```
点击 [驳回]
   │
   ▼
弹驳回 Modal（输入框，必填，≥ 5 字）
   │
   │ 输入驳回理由 → 点击「确认驳回」
   ▼
弹「确认驳回此物料？将通过钉钉通知发起人」二次确认
   │ 取消 → 返回 Modal
   │ 确认
   ▼
后端：
   ① 标记该物料行「已驳回」
   ② 推送钉钉通知 OMS 发起人（含驳回理由）
   ③ MPM 内部清理该行临时数据
   ▼
前端 Toast 「✅ 已驳回：{code}」
   ▼
自动关闭 Tab，OMS 侧该行翻为「已驳回」由销售重推
```

### 2.7 字段权限矩阵

| 字段类型 | 审核员 | 备注 |
|---|---|---|
| 主数据基础（名称/型号/品牌/单位/描述） | ✅ | 修改可能影响五要素查重；改后建议提示「修改可能影响查重结果，是否重新查重？」 |
| 货主 / 编码 / 数据类型 / 标品物料 | ❌ | 系统赋值，只读 |
| 价格类（市场价/商城价等） | ✅ | 见 §2.4.2 |
| 配置类（订单执行员/采购部门/税收等） | ✅（自动赋值，可改） | 类目变更联动 |
| 待询价占位（结算价/项目调拨参考价/参考供货价） | ❌ | 灰字「待询价」 |
| 基本单位 | ⚠ 仅审核阶段可编辑 | 红字提示「审核后无法修改」 |

---

## 3. 类目选择弹窗（核心）

### 3.1 整体布局

```
┌──────────────────────────────────────────────────────────────┐
│ 类目                                                       × │
├──────────────────────────────────────────────────────────────┤
│ [请输入名称或编码___________________________________] 🔍       │
├─────────────────────────────────┬────────────────────────────┤
│ 📂 类目层级       必须选到四级    │ 🤖 四级类目（AI建议） 🔄重试│
│ ─────────────────────────────── │ ┌────────────────────────┐│
│ ⊟ |01| 应急装备                  │ │ ① PVC管道 (02010202)   ││
│   ⊟ |0101| 无人机                │ │   置信度 95% ▰▰▰▰▰▰▰▰▰▱││
│     ⊟ |010101| 多旋翼飞行平台    │ │   💡 商品名称为"PVC线槽"││
│       |01010101| 消费类无人机    │ │   …高度一致             ││
│       |01010102| 教育类无人机    │ │           [✓ 采用推荐] ││
│       |01010103| 行业类无人机    │ ├────────────────────────┤│
│       |01010104| 其它多旋翼…      │ │ ② 塑料管材 (02010203)  ││
│   ⊞ |0102| 消防装备              │ │   置信度 78%   [采用]  ││
│ ⊟ |02| 管材管件                  │ ├────────────────────────┤│
│   ⊟ |0201| 塑料管材              │ │ ③ PVC型材 (02010204)   ││
│     ⊟ |020101| PVC类             │ │   置信度 62%   [采用]  ││
│       |02010101| PVC给排水管     │ └────────────────────────┘│
│       |02010202| PVC管道（已选） │                            │
│       ...                       │                            │
├─────────────────────────────────┴────────────────────────────┤
│                                              [取消]  [确定]   │
└──────────────────────────────────────────────────────────────┘
```

- 弹窗宽 **880px**，主体高 **440px**（左右两栏 flex）
- 左栏 flex:1（最小约 540px），右栏固定 340px

### 3.2 顶部搜索

| 控件 | 行为 |
|---|---|
| 搜索输入框 | `oninput` 实时过滤；支持按**类目名称**或**类目编码**模糊匹配 |
| 🔍 搜索按钮 | 同 oninput 行为；点击触发一次过滤（用于回车/移动端） |
| 命中策略 | 命中节点保留并展开其全部祖先；未命中节点隐藏；空结果显示「未找到匹配的类目」 |

### 3.3 左侧 - 类目层级树

| 表现 | 规则 |
|---|---|
| 节点格式 | `[⊞/⊟] |编码| 名称`，编码用 \| 包裹（参考截图视觉） |
| 缩进 | 一级 16px / 二级 36px / 三级 56px / 四级 76px |
| 四级节点（叶节点） | 灰色文本，无展开图标（`.cat-toggle.leaf` 隐藏） |
| 默认展开 | `expandedNodes` 初始包含 `01, 0101, 010101, 02, 0201, 020101`，含已选节点的祖先链 |
| 节点点击行为 | **非叶节点**：点击切换展开/收起；**叶节点**：选中（`tempSelectedCategory`），高亮蓝底 |
| 选中态 | 蓝底 + 蓝字 + 加粗（`.selected`） |

### 3.4 右侧 - AI 建议区

| 元素 | 规则 |
|---|---|
| 标题 `🤖 四级类目（AI建议）` | 灰色头条 + 右侧「🔄 重试」按钮 |
| 数据源 | 接口4 `POST /api/workflow/material-category-l4-match/invoke` 返回 `data` 顶 3 候选（按 confidence 降序） |
| 单条建议项 | 分上下两层：上层信息（排名图标 + 名称 + 编码 + 置信度进度条 + 判定理由）；下层「采用」按钮右对齐 |
| 排名图标 | ① 绿色（推荐）/ ② 蓝色 / ③ 灰色 |
| 置信度区间 | ≥ 0.90 绿；≥ 0.70 蓝；< 0.70 黄（颜色映射进度条 + 数值色） |
| 判定理由 | `data.reason` 文本；浅灰底 + 左竖条；最多展示，超出滚动 |
| 「✓ 采用推荐」按钮（仅 1 号） | 绿底白字，**点击直接回填类目并关闭弹窗**（含 Toast 反馈） |
| 「采用」按钮（2/3 号） | 白底蓝字，**点击直接回填类目并关闭弹窗**（含 Toast 反馈） |

### 3.5 重试机制

| 步骤 | 行为 |
|---|---|
| 点击 🔄 重试 | 按钮文本变「⏳ 调用中...」 + `pointer-events:none` 防重复 |
| 调用接口4 | 入参取**当前表单实时**的名称 + 规格型号 + 描述（拼接规则同主文档 §11 接口4） |
| 响应回填 | 替换 `aiSuggestions` 为新前 3 候选，重新渲染右栏 |
| Toast | 「AI 类目建议已刷新」 |
| AI 调用失败 | 类目字段留空 + 弹窗提示「AI 服务暂不可用，请稍后重试或手动选择」 |

### 3.6 关闭与确认

| 操作 | 行为 |
|---|---|
| × 关闭 / 取消 / 点击遮罩 | 关闭弹窗，**不修改主表单类目字段** |
| 确定（仅手动选择树时使用） | ① 校验已选到四级（编码长度=8），否则 Toast「请选择到四级类目（最末级节点）」<br>② 回填主表单 `categoryInput`<br>③ 关闭弹窗 + Toast「已选择类目：{name}」 |
| 「✓ 采用推荐」/「采用」 | **跳过确定按钮**，直接执行回填 + 关闭 + Toast |

### 3.7 状态变量（开发参考）

```js
let currentSelectedCategory  = { code, name };  // 主表单已确认的类目
let tempSelectedCategory     = null;            // 弹窗内临时选择
let aiSuggestions            = [...];           // 接口4 返回的前 3 候选
let expandedNodes            = new Set([...]);  // 当前展开的节点 code 集合
```

### 3.8 类目数据结构

```js
const CATEGORY_TREE = [
  { code: '01', name: '应急装备', children: [
    { code: '0101', name: '无人机', children: [
      { code: '010101', name: '多旋翼飞行平台', children: [
        { code: '01010101', name: '消费类无人机' },
        ...
      ]}
    ]}
  ]},
  ...
];
```

> 真实环境下树形需懒加载（每展开一级请求一次后端），原型用静态数据演示。

---

## 4. 字段-接口映射表

> 用于前端开发快速定位「页面字段 → OMS 推送 → MPM 处理 → 接口3 回传」全链路。

### 4.1 主要信息字段映射

| 详情页字段 | OMS 接口1 入参 | MPM 内部 | 接口3 回传 | 主文档锚点 |
|---|---|---|---|---|
| 编码 | — | 审核通过时生成 | `mpmMaterialCode` | §11 接口3 |
| 品牌 | `material.brandId` | 品牌名+编码 | `brandName` + `brandCode` | §11 接口3 |
| 名称 | `material.itemName` | itemName | `itemName` | §11 接口3 |
| 型号 | `material.model` | model | `model` | §11 接口3 |
| 基本单位 | `material.unitId` | 单位名+编码 | `unitCode` + `unitName` | §11 接口3 |
| 类目 | — | 接口4 返回的 `category_name` + `id` | `categoryLevel4` + `categoryLevel4Code` | §11 接口3/4 |
| 订单执行员 | — | 配置表查询 | `orderExecutor` | §11 接口3 |
| 采购部门 | — | 配置表查询 | `purchaseDept` + `purchaseDeptCode` | §11 接口3 |
| 项目报价员 | — | 配置表查询 | `quoter` + `quoterEmployeeNo` | §11 接口3 |
| 产品经理 | — | 配置表查询 | `productManager` | §11 接口3 |
| 加价系数(%) | — | 配置表查询 | （内部使用） | §8.2 |
| 物料税率 | — | 类目联动 | `taxRate` | §11 接口3 |
| 起订量 | `material.quantity` | minOrderQty | `minOrderQty` | §11 接口3 |
| 毛重 | — | grossWeight 默认 1 | `grossWeight` | §11 接口3 |
| 市场价 | 进货单价 × 1.5 | marketPrice | `marketPrice` | §8.2 |
| 商城价 | 自动计算或手动 | mallPrice | `mallPrice` | §11 接口3 |
| 描述 | `material.description` | description | `description` | §11 接口3 |
| 内部备注 | — | 自动写入 `定向函编码：{batchId}` | `internalRemark` | §8.2 |
| 税收分类名称 | — | 类目联动 | `taxCategoryName` | §11 接口3 |
| 税收分类编码 | — | 类目联动 | `taxCategoryCode` | §11 接口3 |

### 4.2 接口4 调用映射（AI 类目）

| 触发时机 | MPM 入参 | 处理 |
|---|---|---|
| 五要素未命中后自动调用 | `name` = `material.itemName`<br>`description` = `material.model + ' ' + material.description` | 写入 `aiSuggestions`，初次渲染右栏 |
| 弹窗内 🔄 重试 | 同上但取**当前表单实时值** | 刷新 `aiSuggestions` |

| 响应字段 | 用途 |
|---|---|
| `data.id` | 类目编码（8 位） |
| `data.category_name` | 类目名称 |
| `data.confidence` | 置信度（0~1，三色映射） |
| `data.reason` | 判定理由（展示在建议项下方） |

> ⚠ 实际接口4 返回单条 `data`，本期前端为展示前 3 候选，需要后端扩展为返回 `data: [{}, {}, {}]` 数组结构（与后端协商）。**主文档 §11 接口4 当前为单条返回，本交互文档对此提出扩展需求**。

### 4.3 通过/驳回触发的接口

| 操作 | 触发接口 | 关键参数 |
|---|---|---|
| 通过 | 接口3 单条回传（MPM → OMS） | `resultSource=AUDIT_NEW`，含全量主数据 |
| 驳回 | 钉钉通知接口 + MPM 内部清理 | 含驳回理由文本 |

---

## 5. 状态机与异常

### 5.1 详情页生命周期

```
打开详情页（带 ?code=LINE_xxx）
   │
   ├── 接口返回 200 → 渲染表单 + 启动引导动画判断 → 正常审核
   │
   ├── 接口返回 410（已被 OMS 撤回）
   │     → 弹窗「该物料已被发起人撤回，无法继续审核」
   │     → 3 秒后自动关闭 Tab
   │
   ├── 接口返回 409（已被其他审核员处理）
   │     → 弹窗「该物料已被 [审核人] 于 [时间] 审核完毕」
   │     → 自动关闭 Tab
   │
   └── 网络异常 / 5xx → Toast「加载失败，请刷新重试」
```

### 5.2 编辑过程异常

| 场景 | 系统处理 | UI 反馈 |
|---|---|---|
| 长时间停留（> 30 分钟无操作） | 心跳检测 | 顶部黄色横幅「页面长时间未操作，建议刷新以同步最新状态」 |
| 接口3 回传失败（自动重试 3 次仍失败） | 主文档 §10.4 | 详情页保持「已审核通过」；操作记录面板显示重试日志，由运维介入 |
| 多 Tab 编辑同一物料 | SessionStorage 检测 | 后开 Tab 弹窗「该物料已在其他 Tab 打开」并自动关闭 |
| 浏览器崩溃 | LocalStorage 自动暂存（每 10 秒） | 重新打开时弹窗「检测到上次未保存的修改，是否恢复？」 |
| AI 接口4 调用失败 | 类目字段留空，红色感叹号 + tooltip「AI 调用失败，请手动选择」 | 不阻断主流程 |

### 5.3 列表页异常

| 场景 | 处理 |
|---|---|
| 全选后某行已被他人审核 | 批量提交时该行返回 410，前端 Toast 跳过提示 |
| 跳转详情页 URL 缺失 `code` 参数 | 弹窗「URL 参数错误，请从列表页进入」 |

---

## 6. 视觉规范摘要

### 6.1 颜色语义

| 颜色 | HEX | 用途 |
|---|---|---|
| 主色蓝 | `#1677ff` | 主按钮、链接、当前 Tab、选中态 |
| 成功绿 | `#52c41a` | 通过按钮、AI 高置信、推荐标签 |
| 警告橙 | `#fa8c16` / `#faad14` | 引导动画、AI 推荐徽标、定向 Tab 引导抖动 |
| 危险红 | `#ff4d4f` | 必填星号、驳回按钮、错误提示、必填校验失败 |
| 紫 | `#722ed1` | 审核类型标签 |
| 灰底 | `#fafafa` | 只读输入框、待询价占位、卡片头部 |
| 黄底 | `#fffbe6` | 价格区背景（敏感字段强调） |
| 绿底 | `#f6ffed` | 类目字段背景（AI 推荐强调）+ AI 推荐项卡片背景 |

### 6.2 字号

| 用途 | 字号 |
|---|---|
| 卡片标题 | 14px / 600 |
| 字段标签 | 12px / 常规 |
| 输入框文本 | 13px |
| 辅助说明 / Toast | 12px |
| 标签徽章 | 10-11px |

### 6.3 间距

| 用途 | 间距 |
|---|---|
| 卡片间距 | 8px |
| 卡片内边距 | 16px |
| 字段网格间距 | 12px / 16px（行 / 列） |
| 表单字段内边距 | 0 10px（输入框） |

---

> **风险提示**：本文档为交互设计层面定义，开发实现前须与后端确认：① 接口4 多候选扩展；② 类目树后端懒加载接口；③ 钉钉驳回通知模板。视觉细节最终以主数据平台 Design Token 为准，本文档颜色/字号仅为参考。
